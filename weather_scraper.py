import requests
"""
此檔案已移動到 `weather_crawler.py`。

為了保持相容性，請改用 `weather_crawler.py` 來執行命令列爬蟲。
舊檔案保留為跳轉說明，以免破壞既有腳本引用。
"""

import sys

print("此檔案已移至 'weather_crawler.py'。請改用該檔案執行命令列爬蟲。")
sys.exit(0)
API_URL = "https://opendata.cwa.gov.tw/api/v1/rest/datastore/F-C0032-001"
